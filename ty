<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Date (Not Asking)</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            background-color: #ffe6f0;
        }
        .container {
            margin-top: 50px;
        }
        button {
            background-color: #ff85a2;
            color: white;
            padding: 15px 32px;
            text-align: center;
            font-size: 20px;
            margin: 10px;
            cursor: pointer;
            border: none;
            border-radius: 5px;
        }
        button:hover {
            background-color: #ff4081;
        }
        .hidden {
            display: none;
        }
    </style>
</head>
<body>

<!-- First Page -->
<div id="page1">
    <h1>Date (Not Asking)</h1>
    <button onclick="goToNextPage('page2')">Ofc, it's literally YOU, and I can't say no to someone so perfect</button>
    <button onclick="goToNextPage('page3')">How can I ever say no to you?! I must be crazy to do that</button>
</div>

<!-- Second Page -->
<div id="page2" class="hidden">
    <h1>Great Choice!</h1>
  
    <button onclick="goToNextPage('page3')">You will do everything as usual, I will just exist and maybe bring drinks so you won't nag like kids</button>
</div>

<!-- Third Page -->
<div id="page3" class="hidden">
    <h1>Yayyyyyy!!</h1>
    <p>Click below</p>
    <button onclick="startConfetti()">Yayyyy Again</button>
</div>

<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.5.1/dist/confetti.browser.min.js"></script>
<script>
    function goToNextPage(pageId) {
        // Hide all pages
        document.querySelectorAll('div').forEach(div => div.classList.add('hidden'));

        // Show the selected page
        document.getElementById(pageId).classList.remove('hidden');
        if (pageId === 'page3') {
            startConfetti();
        }
    }

    // Initial page load - show the first page
    window.onload = function() {
        goToNextPage('page1');
    };
    // Confetti effect
    function startConfetti() {
        var duration = 10 * 10000;
        var animationEnd = Date.now() + duration;
        var interval = setInterval(function() {
            confetti({
                particleCount: 20,
                angle: Math.random() * 360,
                spread: 100,
                origin: { x: Math.random(), y: Math.random() - 0.2 }
            });
            if (Date.now() > animationEnd) {
                clearInterval(interval);
            }
        }, 100);
    }
</script>
</body>
</html>
